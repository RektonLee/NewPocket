行，我们把这四篇当成“你要在组会/跟导师聊得像真的读过”的材料来读：我会给你每篇**在解决什么痛点 → 怎么做 → 关键细节（能被追问的点）→ 结果怎么解读 → 局限/你该怎么用**。你以后被问到细节时，抓住我标的“可被追问点”回答就够了。

---

## 1) ActivityFinder：把 **结构数据(PDB)** 和 **亲和力/活性数据(ChEMBL)** 自动对齐整合（核心是“对齐很脏很难”）

### 这篇在解决什么

现实里你想把“某个蛋白-配体复合物结构”跟“同一个靶点的活性/亲和力测定数据”连起来做训练集（比如 pocket 学习 + affinity 回归），会遇到一个巨坑：

* PDB 里蛋白序列经常是**构建体**：有 tag、缺环、工程突变、非天然氨基酸、编号体系乱七八糟。
* ChEMBL 里是“生物学实体/靶点”的序列或 UniProt 关联。
* 你如果只靠 “PDB 的 UniProt mapping / HET code / 简单 BLAST” 经常**错配**或**丢很多数据**。

ActivityFinder 的目标就是：**尽可能全自动**地把这些对上，并且还能报告“binding site 附近到底改了哪些残基”（因为这直接影响你拿结构去预测活性是否合理）。

### 方法主线（你要能复述的 pipeline）

1. **先用 BLAST 找候选靶点**：用户给一个序列或结构链，ActivityFinder先做 BLAST 搜到 ChEMBL 里可能对应的序列。

2. **再做“更严格的全局比对”去处理构建体差异**：光 BLAST 不够，因为 tag、缺失片段会让局部比对很碎。

   * 他们做了一个带 **affine gap cost（开 gap 和延长 gap 分开计价）** 的最优全局比对，并强调要把“构建体序列”和“参考序列”对齐得更像人类会对齐的样子（比如 tag 应该是一段 gap，而不是被强行匹配成很多单点）。这块是他们认真打磨的工程核心：用一套成本参数让对齐更稳。
   * 还有一个很工程的点：他们把 PDB ATOM 的三字母残基编码和一字母序列在对齐时做“lazy translation”，减少 bookkeeping。

3. **ATOM / SEQRES / SEQADV 三种序列的麻烦全处理**

   * ATOM：有坐标、有编号，但可能缺失未解析区域。
   * SEQRES：是“理论序列”，但没有 ATOM 的编号体系。
   * SEQADV：记录“要把构建体还原成参考序列需要替换哪些残基”，但它的编号跟 ATOM 走。
     所以他们需要建立 **ATOM ↔ SEQRES 的映射**，才能把结构上的 pocket 残基追溯到参考序列，再去对 ChEMBL。

4. **binding site mutation 只盯 pocket 附近**

   * 他们不是报告全蛋白所有差异，而是只显式登记**与结合位点相关**的插入/缺失/错配。
   * 定义 binding site：以配体每个原子为中心，取直径 6.5 Å 的球；任何残基/共价小分子只要原子落入或被包围都算 binding site。
     这个定义你要记住：导师很可能会问“他们怎么定义 pocket/结合位点范围”。

5. **两种 target matching query 模式**

   * 模式1：对蛋白所有链都尝试匹配（宽松）。
   * 模式2：只对“属于该配体 binding site 的链”做匹配（更严格、减少假阳性）。

### 结果与比较（你要会讲“为什么他们的链接更多/不同”）

他们把结果与 BioChemGraph 比较，用 (PDB ID, ChEMBL target ID, ChEMBL molecule ID) 三元组评估。ActivityFinder 找到的 triplet 数更多：BioChemGraph 19333，ActivityFinder 46287，重叠 14771。
并解释了哪些是 BioChemGraph 独有、哪些是 ActivityFinder 独有：比如 ActivityFinder 当前只考虑 X-ray 结构，而 BioChemGraph 还纳入 EM/NMR；以及“BioChemGraph 用 HET code 表征配体 vs ActivityFinder 用 HETATM 重建出的真实建模结构”，会导致同 HET code 不同实例的化学结构差异。

### 局限/你该怎么用（给你“像读过”的评价）

* **序列相似度阈值**：他们提到常用 90–100% 处理近同源，但为了允许工程突变/标签/同工型，数据库构建时用到 **80% 下限**也合理；用户查询时还可以设更高阈值。
* **这篇对你做 enzyme pocket 学习的直接价值**

  * 你现在的 pipeline 里“同一 UniProt 可能多条样本共享 pocket 坐标”那套思路，本质上也依赖“结构-序列-靶点”的干净映射。ActivityFinder 的贡献是把这件事做成更可靠的自动流程，并且**输出 pocket 附近突变**，这对你训练 GNN/3D 模型时做“样本可信度分层”非常有用：

    * pocket 无突变/小突变：高可信训练样本
    * pocket 大改动/tag 影响 pocket：要么剔除，要么作为 domain shift 样本单独处理

---

## 2) SwinSite：用 **3D voxel + CNN + Swin Transformer** 做结合位点预测（核心是“局部几何 + 长程依赖”）

### 这篇在解决什么

binding site detection 很关键：口袋找错会让 docking/虚拟筛选的搜索空间爆炸，后面全链路都受影响。SwinSite 的主张是：

* 纯 3D CNN 擅长局部几何，但捕捉长程依赖要堆很深，容易大模型/过拟合。
* Transformer 擅长全局依赖，但直接做 3D 全局 attention 太吃显存。
  所以用 **Swin Transformer（窗口注意力+shifted window）** 来在 3D 里高效建模“中远程空间关系”。

### 数据与表示（可被追问点：voxel 的尺度、通道、标签怎么做）

* 数据：scPDB 子集 5020 个 protein-ligand complex（和 PUResNet 相同训练子集）。
* 对齐与增强：把蛋白质质心放到原点，做 24 种正交旋转增强。
* voxel 网格：空间范围 ±35 Å，分辨率 1.5 Å，网格 96×96×96。
* 每个重原子映射到 voxel 的特征通道：**18 维**，包括原子类型、杂化、邻居数、估计部分电荷、芳香性/氢键能力等（注意他们明确把“电荷”等物化特征塞进 voxel 通道）。
* **高斯平滑嵌入**：原子对周围 voxel 的贡献用 3D Gaussian kernel。
* **标签（监督信号）不是硬 0/1**：把配体原子也投到 voxel 上做 Gaussian map，然后再做一次形态学膨胀（相当于把正例扩张到“离配体原子 3 Å 左右”），再 clip 到 [0,1]，得到更稳定的监督信号（这对训练非常关键：边界更平滑，不容易学成锯齿）。

### 模型结构（可被追问点：U-Net、Swin block、shift window）

* 总体：U-Net 式 encoder-decoder。encoder 交替用 ConvBlock3D（抓局部几何）和 SwinBlock3D（抓更大范围上下文），decoder 用 skip connection 还原空间分辨率。
* SwinBlock3D：窗口注意力（W-MSA）+ shifted-window 注意力（SW-MSA）交替，让相邻窗口的信息能交换，又不做全局 attention。

### 结果怎么讲

你不用背具体数字，但要会说“他们在多个 benchmark 上稳定比 CNN/GNN pocket detector 更强、泛化更好”，并强调他们认为这是因为能同时编码**局部 cavity 形状**与**全局结构上下文**。

### 对你有什么用

* 你现在做的是“蛋白 pocket + 底物”的图学习；SwinSite 是“只用蛋白结构找 pocket”。你可以把它当：

  1. 预处理模块：先用 SwinSite 给候选 pocket 区域，再做 docking/图构建；
  2. baseline：证明“只用结构也能找到 pocket”，你的方法要比它更进一步（结合底物/动力学信息）。

---

## 3) PPB3：用 ChEMBL 巨量数据做 **小分子→多靶点** 预测（核心是“像相似性搜索，但用 DNN 扩展到 7546 targets”）

### 这篇在解决什么

多靶点/脱靶（polypharmacology）是常态。以前很多 web 工具只覆盖少量 targets 或只限单蛋白靶点。PPB3 的目标是：把 ChEMBL 里非常大规模的 molecule-target 互动数据做成一个**在线预测器**，能覆盖更多 target type（单蛋白、复合物、蛋白家族、细胞系、物种等）。

### 数据与标签（可被追问点：怎么定义“active”，负例怎么来的）

* 只保留：每个 target 至少 5 个活性分子；分子大小 ≤80 个非氢原子；排除 Unknown targets。
* “活性”阈值：Ki/IC50/EC50 等 ≤10 μM（并强调这是为了做 general classifier 找可能靶点/脱靶，而不是回归精确 potency）。
* 输出是 7546-bit 的 target fingerprint；一个 compound-target 没数据或没过阈值就当作 “inactive”。

### 模型（非常朴素但工程有效）

* 输入：7 种二值分子指纹之一（6 个 4096-bit 或 1 个 8192-bit fused）。
* 网络：MLP，两层隐藏层（1000 和 500 神经元）。
  他们明确说：不是不知道 GNN/Transformer，而是为了 web 可用性与已验证性能，沿用 MLP。

### 最关键结论（你要像读过一样把“适用范围”说清楚）

**预测好坏强依赖“你这个分子在训练集中有没有结构相近的邻居”**。
他们用 ECFP4 Tanimoto 相似度衡量 test 分子到训练集最近邻的相似性，发现 recall/precision 在相似度 >0.4 才明显起来，>0.7 逐渐接近上限。
这句话你可以用来回答导师的追问：“这种大规模 target predictor 会不会只是相似性搜索换皮？”——你可以说作者自己也承认性能和相似度高度相关，所以**对新化学骨架（low similarity）要谨慎**。

### 工程化与使用建议（可被追问点：怎么用 top-k 和阈值）

* web tool：后端 Flask + nginx；预测 <1s；无 public API；模型可下载。
* 使用建议：看 top-5 到 top-20，最好用 consensus model，并可用 20% 概率阈值过滤。
* 对 macrocycle/peptide 这类在训练数据中占比低的化合物要谨慎。

### 跟你有什么关系

你在做“底物 SMILES/结构 + 蛋白 pocket → 预测动力学”。PPB3 的思路可以给你一个旁路 baseline：

* **只用底物结构**预测“可能作用的靶标类型/家族”，作为你数据清洗或负采样的辅助信息；
* 但你也要意识到它对“新底物骨架”可能不稳，这个 limitation 你如果在论文里提一嘴，会显得你真的读过。

---

## 4)《Advances in ML models for predicting enzyme kinetic parameters》：综述你要当“答辩/开题用的武器库”

这篇是综述，导师让你读通常是为了：你要能把“别人怎么预测 kcat/Km、数据从哪来、难点是什么、未来方向是什么”讲清楚。

### 综述的总体结构（你可以直接复述）

作者明确说：先讲 ML 基础（Section 2），再讲公开数据集（Section 3），再系统总结模型输入/架构/能力（Section 4），再讲应用（Section 5）和特定家族 local models（Section 6），最后讲挑战与未来方向（Section 7）。

### “输入表示”这块，你要讲得像懂行（能被追问点）

他们把 enzyme kinetics 预测的输入归结为两大模态：

* **酶**：序列或 3D 结构
* **底物**：化学结构/SMILES/指纹
  并强调不同神经网络擅长不同模态编码，但通常需要足够数据。

然后他们点名了几种序列编码路线：

* one-hot：直观但稀疏高维、效率差。
* CNN：能抓局部 motif，但对长程依赖弱。
* protein language model（pLM）：用 transformer 通过自监督学到上下文表示来补长程依赖问题。

### 他们对“全局模型 vs 局部模型”的判断（你一定会被导师问）

这是这篇综述最“观点型”的部分之一：

* **Global models**：覆盖很多酶家族/大范围序列空间，泛化范围大；但对训练集里很少见、与训练分布差异大的序列，精度会掉。
* **Local models**：针对特定家族、野生型/突变体的高质量数据，能学到更细的结构-功能关系；但外推范围窄。
* 他们提出一个明确方向：**hybrid**——用全局 pLM 表示做 broad context，再在家族特定 kinetic 数据上 fine-tune 保留 local 信息。

你如果要把自己毕设/项目放进去，一句话就能对齐：

> “我现在做的其实偏 local（特定反应/底物/口袋），但我想借助全局 pLM 或结构模型来提升跨酶种的可迁移性，这跟综述里提的 hybrid 方向一致。”

### 未来方向（你要能抛出来显得很懂）

综述里比较“硬”的未来方向有两个你很适合用：

1. **半监督/弱监督**：当前大多是纯监督，但 labeled kinetic 数据很少；可以用 UniProt 海量无标注序列配合少量 kinetic 标签做半监督，提高鲁棒性。
2. **融入物理约束的 ML（physics-based ML）**：例如把 transition state theory 里 activation free energy 与 kcat 的关系作为约束/正则项，或者结合 QM/MM 描述符，让模型不只是拟合数据而是更“机制一致”。

这两条你都能自然接到你当前方向：

* 你有结构预测（ESMFold/AlphaFold 类）+ docking + pocket 图 → 这天然就可以做 **semi-supervised（结构/序列预训练 + 少量 kcat/Km 标签）**；
* 你又是 AI4S，讲 physics-based ML 会很加分：至少能说清“我们希望预测结果与能垒/自由能范围一致”。

---

# 给你一套“30 秒装作读过”的口头总结模板（你照着说就很像）

你跟导师/组会这样开场：

* **ActivityFinder**：它解决的是 PDB 构建体序列和 ChEMBL 靶点序列难对齐的问题，用 BLAST 找候选后再用带 affine gap 的全局比对把 tag/缺环/突变处理得更像人工对齐，并且只报告 pocket 附近的突变（6.5 Å 直径球定义 binding site），最后能生成更多可靠的 PDB–ChEMBL triplet，用于结构-活性整合训练集。
* **SwinSite**：它做 3D 口袋预测，把蛋白 voxelize 成 96³、1.5 Å 分辨率、18 通道原子理化特征，用 U-Net encoder-decoder，CNN 抓局部几何，Swin Transformer 的 shifted window attention 抓长程依赖，标签用 Gaussian + 形态学膨胀提供平滑监督，因此在多个 benchmark 上比传统 CNN/GNN 稳定更好。
* **PPB3**：它用 ChEMBL 34 巨量数据训练 MLP，把分子指纹映射到 7546 维 target 指纹，适合快速 polypharmacology/off-target 初筛；但性能高度依赖测试分子在训练集是否有相似近邻（Tanimoto >0.4 才明显好），所以对新骨架要谨慎。
* **酶动力学综述**：总结了 kcat/Km 预测常见的输入（序列/结构 + 底物 SMILES/指纹）、模型路线（CNN/pLM/结构模型等）、并强调 global vs local 的 tradeoff，推荐 hybrid（全局 pLM + 家族微调），以及 semi-supervised 和 physics-based ML 是未来方向。

---

如果你愿意，我们下一步可以更“像备考”一点：我按你毕设/项目（pocket+substrate 图、docking、kcat/Km）给你整理一张“**这四篇能直接写进 related work 的段落骨架**”（每篇 4–6 句：贡献+方法+局限+与本文关系），你直接复制到开题/论文里。
