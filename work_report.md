# 酶动力学预测项目工作报告

## 📅 日期：2024年9月28日

## 🎯 当前任务：构建kcat预测模型的数据集

### ✅ 已完成工作

#### 1. 数据处理与整合
- **合并成功预测结果**：从8个并行chunk中合并了4,399个成功预测的样本
- **数据修正**：使用原始实验值替换预测值，确保数据真实性
- **文件结构适配**：处理了新的PDB文件组织结构（`sample_data/samples/{sample_id}/`）
- **列名标准化**：将`smiles`列重命名为`substrate_smiles`以匹配训练脚本

#### 2. 构图逻辑分析
- **节点特征**：52维原子表征（元素+残基+物理属性+电子结构）
- **边特征**：Gaussian RBF距离编码（16个中心）
- **图构建**：4.0 Å距离阈值，适合分子内相互作用
- **全局特征**：标准化温度信息

#### 3. 数据质量验证
- **PDB文件存在性**：确认所有4,399个样本都有对应的PDB文件
- **数据完整性**：包含sample_id、sequence、substrate_smiles、kcat_value、ec、temperature
- **标签真实性**：使用原始实验kcat值，范围3e-06到440,000

### 🔧 技术实现

#### 构图脚本：`build_graph_dataset.py`
- 适配新的文件结构：`sample_data/samples/{sample_id}/{sample_id}_{hash}_10A.pdb`
- 复用`graph_builder_rbf.py`的构图逻辑
- 添加EC信息到图数据中
- 处理缺失Km值（设为1.0占位符）

#### 数据流程：
```
CSV数据 → 图构建 → PyTorch Geometric Data → .pt文件 → 训练脚本
```

### ⚠️ 当前问题

#### 1. 环境配置
- **Conda环境**：需要激活env2环境才能运行PyTorch
- **依赖库**：缺少torch-cluster，使用替代方法构建边

#### 2. 模型架构适配
- **标签格式**：当前模型期望[kcat, km]两个值，但数据只有kcat
- **模型修改需求**：需要设计单目标预测模型（只预测kcat或km）

### 🚀 下一步计划

#### 1. 立即任务
- [ ] 解决conda环境问题，成功运行图构建脚本
- [ ] 生成`kcat_dataset_for_training.pt`文件
- [ ] 验证图数据质量

#### 2. 模型设计
- [ ] 修改GNN模型以支持单目标预测
- [ ] 设计kcat专用预测模型
- [ ] 设计km专用预测模型（后续）

#### 3. 训练优化
- [ ] 调整损失函数（MSE → Huber Loss）
- [ ] 优化超参数
- [ ] 实现早停和模型保存

### 📊 数据统计
- **总样本数**：4,399个
- **EC数量**：624个不同EC编号
- **kcat范围**：3e-06 到 440,000
- **成功率**：100%（所有样本都有PDB文件）

### 🔬 科学合理性
- **原子表征**：全面且科学，包含物理化学属性
- **边构建**：距离阈值合理，RBF编码连续
- **温度考虑**：符合酶动力学理论
- **配体区分**：重要的生物学特征

### 📝 备注
- 当前专注于kcat预测，km预测模型将在后续开发
- 图构建逻辑科学合理，符合分子建模最佳实践
- 数据质量高，适合深度学习训练

