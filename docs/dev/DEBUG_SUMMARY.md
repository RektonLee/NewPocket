# 模型调试总结报告

## 🔍 问题诊断

### 1. 核心问题
**模型输出几乎为常数**，无法区分不同样本：
- 预测值范围：[0.21, 0.98]（非常窄）
- 真实值范围：[-0.76, 2.67]（测试集）或 [-5.54, 6.03]（训练集）
- 预测标准差：0.1282（只有真实标准差的1/4）

### 2. 模型表现
- **测试集**：MAE=0.5558, RMSE=0.6696, R²=-0.6591, Pearson=0.0524
- **训练集**（前1000样本）：MAE=1.4179, RMSE=1.8254, R²=-0.0013, Pearson=0.0754
- **结论**：模型在训练集和测试集上都表现很差，说明模型本身没有学好

### 3. 内部激活分析
- **图embedding**：mean=-0.0100, std=0.1935（方差很小，不同样本的图表示很相似）
- **MLP最后一层输出**：几乎为常数（所有样本输出≈0.5954）
- **问题根源**：图embedding没有学到有区分度的特征

### 4. 数据分布差异
- **训练集**：mean=0.5732, std=1.6088, range=[-5.54, 6.03]
- **测试集**：mean=0.9924, std=0.5200, range=[-0.76, 2.67]
- **差异**：测试集范围更窄、均值更高，但这不是主要问题（模型在训练集上也表现差）

## 🔧 已完成的修复

1. ✅ **模型结构适配**：支持有/无LayerNorm的模型
2. ✅ **自动检测**：自动识别模型结构（通过检查mlp.0.weight维度）
3. ✅ **测试脚本**：完整的测试和调试脚本

## 🚨 需要进一步调查的问题

### 1. 模型训练问题
- 为什么图embedding的方差这么小？
- 训练过程中是否出现了梯度消失？
- 学习率是否合适？
- 是否有训练日志可以查看？

### 2. 数据问题
- 图特征是否包含足够的信息？
- 是否需要检查图构建过程？
- 24维边特征是否正确计算？

### 3. 模型架构问题
- hidden_dim=128是否足够？
- num_layers=3是否足够？
- 是否需要更深的网络或更大的hidden_dim？

## 📝 建议的下一步

1. **检查训练日志**：查看训练过程中的loss曲线、梯度范数等
2. **检查图特征**：验证24维边特征是否正确计算
3. **尝试其他模型**：测试是否有表现更好的模型checkpoint
4. **重新训练**：如果确认是训练问题，可能需要重新训练模型

## 📊 当前测试结果

### kcat_after_new模型
- 测试集：MAE=0.5558, RMSE=0.6696, R²=-0.6591, Pearson=0.0524
- 预测范围：[0.21, 0.98]（过窄）
- 问题：模型输出几乎为常数，图embedding方差很小（std=0.1935）

### kcat_20251213_151558模型
- 测试集：MAE=1.0493, RMSE=1.3729, R²=-5.9742, Pearson=0.0088
- 预测范围：[-4.83, 5.93]（范围更宽，但预测不准确）
- 问题：虽然输出范围宽了，但预测仍然不准确，R²为负

### 结论
- **所有测试的模型在测试集上表现都很差**（R²为负或接近0）
- **主要问题**：模型没有学到有效的特征表示
- **可能原因**：
  1. 训练过程出现问题（梯度消失、学习率不当等）
  2. 图特征本身信息不足
  3. 模型架构不适合这个任务
  4. 需要检查训练日志和重新训练
