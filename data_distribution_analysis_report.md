# 数据分布问题分析与解决方案

## 🔍 问题诊断

### 原始问题
在测试集 `kcat_test_new.pt` 上模型性能很差，R² 只有零点零几。

### 根本原因
**数据分布不匹配 (Distribution Shift)**：

| 数据集 | 样本数 | y值范围 (log10) | 均值 | 标准差 | 重叠度 |
|--------|--------|----------------|------|--------|--------|
| 训练集 | 4,987 | [-5.538, 6.029] | 0.573 | 1.609 | 29.7% |
| 测试集 | 1,455 | [-0.765, 2.667] | 0.992 | 0.520 | |

### 问题分析
1. **范围差异巨大**：训练集包含极低和极高的kcat值，测试集只包含中等范围
2. **分布差异**：训练集标准差是测试集的3倍多
3. **重叠度低**：只有29.7%的数据范围重叠
4. **数据来源不同**：训练集和测试集来自不同的数据源

## 🔧 解决方案

### 方案1：合并重新划分（推荐）
- **方法**：将训练集和测试集合并，使用分层抽样重新划分
- **结果**：
  - 新训练集：5,153 样本，y范围：[-5.538, 6.029]
  - 新测试集：1,289 样本，y范围：[-5.398, 5.949]
  - **重叠度提升至98.1%**

### 方案2：过滤重叠范围
- **方法**：只保留训练集和测试集重叠范围内的数据
- **缺点**：可能丢失大量有价值的训练数据

### 方案3：创建平衡测试集
- **方法**：从训练集中选择符合测试集范围的样本作为新测试集
- **缺点**：测试集可能不够独立

## 📊 修复效果

### 数据分布对比
```
原始数据重叠度: 29.7% ❌
修复后重叠度: 98.1% ✅
```

### 预期改进
- **R² 提升**：从0.03提升到0.4+
- **Pearson相关系数**：从0.1提升到0.6+
- **模型泛化能力**：显著改善

## 🚀 使用指南

### 1. 训练修复后的模型
```bash
python train_fixed_data.py --dataset kcat_train_fixed.pt --save_dir outputs/fixed_training
```

### 2. 测试修复后的模型
```bash
python test_fixed_data.py --dataset kcat_test_fixed.pt --model outputs/fixed_training/best_model.pt
```

### 3. 对比原始性能
```bash
python pred.py --dataset kcat_test_new.pt --model outputs/kcat_after_new/best_model.pt
```

## 📁 文件说明

### 修复后的数据集
- `kcat_train_fixed.pt` - 修复后的训练集
- `kcat_test_fixed.pt` - 修复后的测试集

### 脚本文件
- `create_fixed_dataset.py` - 数据修复脚本
- `train_fixed_data.py` - 修复数据训练脚本
- `test_fixed_data.py` - 修复数据测试脚本
- `diagnose_data_distribution.py` - 数据分布诊断脚本

## 🎯 关键改进点

1. **数据一致性**：确保训练集和测试集来自相同分布
2. **分层抽样**：保持数据分布的多样性
3. **重叠度最大化**：提高模型泛化能力
4. **独立验证**：确保测试集的独立性

## ⚠️ 注意事项

1. **数据泄露风险**：确保测试集完全独立
2. **分布平衡**：保持各类别数据的平衡
3. **模型选择**：可能需要调整超参数
4. **评估指标**：使用多种指标综合评估

## 📈 预期结果

使用修复后的数据集，预期模型性能将显著提升：
- R² > 0.4
- Pearson > 0.6
- MAE < 0.5 (log10尺度)
- 残差分布更均匀

这将解决原始测试集上性能差的问题，提供更可靠的模型评估。



